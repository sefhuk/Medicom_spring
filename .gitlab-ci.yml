stages:
  - build
  - test

job_build:
  stage: build
  script:
    - echo "Building the project"
    - chmod +x gradlew
    - gradlew build

  artifacts:
    paths:
      - build/libs/*.jar

job_test:
  stage: test
  script:
    - echo "Running tests"
