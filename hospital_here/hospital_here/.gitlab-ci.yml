stages:
  - build
  - test

job_build:
  stage: build
  before_script:
    - echo 'build starts'
    - chmod +x ./gradlew
  script:
    - ./gradlew clean build
  artifacts:
    paths:
      - build/libs/*.jar

job_test:
  stage: test
  script:
    - echo "Running tests"
